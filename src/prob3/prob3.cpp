#include<iostream>
#include<vector>


std::vector<int> primes(int n) {
    std::vector<int> nums(n);
    std::iota(n.begin(), n.end(), 1);

    nums[0] = -1
    nums[1] = -1
    
    for (int i = 0; i <= n; i++) {

    }
    
